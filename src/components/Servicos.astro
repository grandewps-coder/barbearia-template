---
import servicos from "../data/servicos.json";

import Container from "./ui/Container.astro";
import Card from "./ui/Card.astro";
import Section from "./ui/Section.astro";
import SectionTitle from "./ui/SectionTitle.astro";
---

<Section id="servicos">

  <Container>

    <SectionTitle
      title="Nossos Serviços"
      subtitle="Cuidados masculinos com qualidade, técnica e atenção aos detalhes."
    />

    <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">

      {
        servicos.map((servico, index) => (

          <Card class="group overflow-hidden border border-neutral-200 transition-all duration-500 hover:-translate-y-3 hover:border-amber-300 hover:shadow-2xl">

            <div>

              <div class="mb-6 flex h-16 w-16 items-center justify-center rounded-2xl bg-gradient-to-br from-amber-100 to-amber-200 transition duration-300 group-hover:scale-110">
                <span class="text-3xl">

                  {
                    index === 0 ? "✂" :
                    index === 1 ? "✦" :
                    index === 2 ? "🪒" :
                    index === 3 ? "◉" :
                    "◆"
                  }

                </span>

              </div>

              <h3 class="mb-3 text-2xl font-bold">

                {servico.nome}

              </h3>

              <p class="mb-6 text-neutral-600 leading-relaxed">

                {servico.descricao}

              </p>

              <span class="inline-flex items-center gap-2 font-semibold text-amber-700 transition-all duration-300 group-hover:translate-x-2">

                Atendimento Premium →

              </span>

            </div>

          </Card>

        ))
      }

    </div>

  </Container>

</Section>